# Local terraform directories
.terraform/

# .tfstate files contain the actual blueprint mapping of your live cloud resources.
# In a corporate setting, these are stored in a secure remote bucket, NEVER on GitHub.
*.tfstate
*.tfstate.*

# Crash logs
crash.log
crash.*.log

# Override files
override.tf
override.tf.json

# Local environment variables/secrets
.terraformrc
terraform.rc